
int buzzPin=11;
int dt1=1;
int dt2=2;
int j;

void setup() {
  // put your setup code here, to run once:
  pinMode(buzzPin,OUTPUT);
  
}

void loop() {
  // put your main code here, to run repeatedly:
  analogWrite(buzzPin,0);
  delay(100);
  analogWrite(buzzPin,5);
  analogWrite(buzzPin,15);
  analogWrite(buzzPin,25);
  analogWrite(buzzPin,35);
  analogWrite(buzzPin,45);
  analogWrite(buzzPin,65);
  analogWrite(buzzPin,75);
  analogWrite(buzzPin,85);
  analogWrite(buzzPin,95);
  analogWrite(buzzPin,115);
  analogWrite(buzzPin,125);
  analogWrite(buzzPin,135);
  analogWrite(buzzPin,145);
  analogWrite(buzzPin,155);
  analogWrite(buzzPin,165);
  analogWrite(buzzPin,175);
  analogWrite(buzzPin,185);
  analogWrite(buzzPin,195);
  analogWrite(buzzPin,205);
  analogWrite(buzzPin,215);
  analogWrite(buzzPin,225);
  
  
  }

  
